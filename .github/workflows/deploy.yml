name: Deploy Slides to GitHub Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: bc2026/package-lock.json

      - name: Install bc2026 dependencies
        run: npm ci
        working-directory: bc2026

      - name: Build bc2026 slides
        run: npm run build -- --base /slideshow/
        working-directory: bc2026

      - name: Install a11y dependencies
        run: npm ci
        working-directory: a11y

      - name: Build a11y slides
        run: npm run build -- --base /slideshow/a11y/
        working-directory: a11y

      - name: Install dev_wars dependencies
        run: npm ci
        working-directory: dev_wars/codemotion2025

      - name: Build dev_wars slides
        run: npm run build -- --base /slideshow/dev_wars/
        working-directory: dev_wars/codemotion2025

      - name: Merge talk dists into bc2026 dist
        run: |
          mkdir -p bc2026/dist/a11y
          cp -r a11y/dist/. bc2026/dist/a11y/
          mkdir -p bc2026/dist/dev_wars
          cp -r dev_wars/codemotion2025/dist/. bc2026/dist/dev_wars/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: bc2026/dist

  deploy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
